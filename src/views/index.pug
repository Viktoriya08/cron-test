doctype html
html
	head
		meta(charset="UTF-8")
		meta(name="viewport" content="width=device-width, initial-scale=1.0")
		link(rel="icon" type="image/x-icon" href=`${img}/favicons/favicon.ico`)
		title=projectName
		include /base/_mixins
	body
		.bg
		.content
			.logo
				+image('img/logo.svg', 'alt').logo__img
			.block
				.block__title Тестовое задание Гнездилова Виктория
				ul.block__list
					li
						a(href="./main.html") Главная страница


		style(type="text/css").
			:root {
				/* Цветовая гамма проекта */
				--main-color: #fb6542;
			}
			body {
				background: #E9E9E9;
				margin: 0;
			}
			.bg {
				position: absolute;
				left: 0;
				right: 0;
				width: 100%;
				height: 100%;
				overflow: hidden;
				z-index: -1;
			}
			.bg::after {
				content: '';
				position: fixed;
				top: 0;
				right: 0;
				width: 50%;
				height: 50%;
				transform: translateX(40%) translateY(-40%);
				background: linear-gradient(209.1deg, var(--main-color) 24.34%, var(--main-color) 73%, var(--main-color) 93.04%);
				filter: blur(250px);
			}
			.content {
				max-width: 1280px;
				width: 100%;
				margin-left: auto;
				margin-right: auto;
				padding: 60px 20px;
				box-sizing: border-box;
				font-family: Verdana, Geneva, sans-serif;
				color: #141414;
			}

			.logo {
				margin-bottom: 90px;
			}

			.logo__img {
				width: 200px;
				display: block;
			}

			.logo img {
				display: block;
				max-width: 100%;
			}
			.block {
				margin-bottom: 40px;
			}
			.block + .block {
				border-top: 1px solid #D7D7D7;
				padding-top: 40px;
			}

			.block__title {
				margin-bottom: 20px;
				font-size: 17px;
				line-height: 20px;
				font-weight: 600;
			}
			.block__list {
				list-style: none;
				padding: 0;
				font-weight: 400;
				font-size: 16px;
				line-height: 20px;
			}
			.block__list li {
				display: flex;
				align-items: center;
			}
			.block__list li::before {
				content: '';
				width: 10px;
				height: 10px;
				background: #c3c3c3;
				border-radius: 3px;
				margin-right: 12px;
			}
			.block__list li.done::before {
				background: var(--main-color);
			}

			.block__list a {
				text-decoration: none;
				color: #141414;
			}
			.block__list li:not(:last-child) {
				margin-bottom: 12px;
			}
